# TODO

### TODO
- It should be possible to add pictures to an item by browsing files on the computer. This should upload the file to the server's filesystem and allow it to be displayed on the page

## Routes
- Add a `/routes/current` endpoint that the Routes button in the top bar redirects to the latest route in the current cruise. The latest route is the one with the highest ID.
  Doesn't work yet, SQL query error in `current_route`
